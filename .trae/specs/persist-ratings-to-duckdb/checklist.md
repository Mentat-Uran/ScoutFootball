- [x] GPU 服务器 `/download/<path>` 端点可正常下载文件，且只允许下载指定目录
- [x] `gpu_client.py download` 能将评分产物下载到本地 `data/gold/feature_store/`
- [x] `scoutlab export-ratings` 生成 `data/gold/scoutlab.duckdb`，包含 4 张表
- [x] `player_ratings` 表行数与 `player_ratings_optimized.parquet` 一致（13968 行）
- [x] `model_meta` 表包含最新的优化指标（spearman, pearson, overfit_gap）
- [x] `league_metrics` 表包含 5 大联赛的 holdout 指标
- [ ] `team_coverage` 表包含各联赛赛季的覆盖率和置信度（当前为空，因 rating_team_coverage.parquet 不在服务器上）
- [x] `load_player_ratings()` 能按位置/联赛/球队筛选返回数据
- [x] DuckDB 不存在时 `load_player_ratings()` 优雅降级
- [x] `ruff check` 零错误（核心文件）
- [x] `pytest` 全部通过（293 passed）
- [x] 重复运行 `export-ratings` 结果幂等
